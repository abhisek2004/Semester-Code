#include<stdio.h>
void sort(int [],int);
int main()
{
    int arr[10],i;
    printf("\nenter the element into array");
    for(i=0;i<9;i++)
}




printf("\nTravers the original array");
for(i=0;i<=9;i++)
{
    printf("\narr[%d]=%d",i,arr[i]);
}
sort(arr,10);
return 0;

    void sort(int arr[],int l)
{
    int i,j;
    int t;
    for()
}
for(j=i+l;j<l;j++)
{
    if(arr[i]>arr[j])
    {
        t=arr[i];
        arr[i]=arr[j];
        arr[j]=t;
    }
    printf("\nTraverse the sort")
}